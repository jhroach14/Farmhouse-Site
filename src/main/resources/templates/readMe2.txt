Author: James Roach 8/3/17
These readMe's will be distributed throughout the project to document the project structure and
give general context to directories/files

SPRING MVC:
 -> view (you are here)
    controller
    model

this directory /resources/templates is where thymeleaf looks by default for html templates it can
populate with data. In this project's implementation of the spring MVC architecture the
view(data sent to client) is represented by dynamically generated thymeleaf templates